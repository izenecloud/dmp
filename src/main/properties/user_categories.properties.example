# hosts: URIS to couchbase servers
hosts = couchbase1,couchbase2

# bucket: couchbase bucket
bucket = bucketname

# password: couchbase password (if required)
password = secret

# batchSize: couchbase commit batch size
batchSize = 10000

# basedir: HDFS path to logs directory
basedir = /path/to/logs

# vim:ft=jproperties:nospell:
